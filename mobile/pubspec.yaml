name: hemal_training_app
description: Mobile application for Hotel Training Management System.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  qr_code_scanner: ^1.0.1
  firebase_core: ^2.15.0
  firebase_messaging: ^14.6.5
  provider: ^6.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
