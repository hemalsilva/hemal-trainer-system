const fs = require("fs"); const file = "backend/routes/employees.js"; let content = fs.readFileSync(file, "utf8"); content = content.replace("`WHERE EXTRACT(MONTH FROM t.training_date) = ${paramIndex} AND EXTRACT(YEAR FROM t.training_date) = ${paramIndex+1}`", "`WHERE EXTRACT(MONTH FROM t.training_date) = $${paramIndex} AND EXTRACT(YEAR FROM t.training_date) = $${paramIndex+1}`"); content = content.replace("`WHERE EXTRACT(MONTH FROM assessment_date) = ${paramIndex} AND EXTRACT(YEAR FROM assessment_date) = ${paramIndex+1}`", "`WHERE EXTRACT(MONTH FROM assessment_date) = $${paramIndex} AND EXTRACT(YEAR FROM assessment_date) = $${paramIndex+1}`"); fs.writeFileSync(file, content); console.log("Done");
